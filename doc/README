
Install the Sphinx document generator in a Python virtual environment on your
system.

Export the UNSILO_WEBAPP_SPHINX_BUILD environment variable, pointing to the
sphinx-build executable, in your .bashrc script.

Execute build.sh
